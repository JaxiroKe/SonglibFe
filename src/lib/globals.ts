const appServer = process.env.NEXT_PUBLIC_SERVER;

export const categoriesUrl = `${appServer}category`;
export const productsUrl = `${appServer}product`;
export const userAccountsUrl = `${appServer}user`;
export const userAddressUrl = `${appServer}address`;
export const userCartUrl = `${appServer}cart`;
export const userProfileUrl = `${appServer}profile`;
export const userWalletUrl = `${appServer}wallet`;
export const userWishlistUrl = `${appServer}wishlist`;
